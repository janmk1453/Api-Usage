{
  "name": "api-usage-stat",
  "private": true,
  "version": "3.0.9",
  "type": "module",
  "description": "API用量统计 — SillyTavern 原生扩展（DeepSeek 官方浅色风格，内容与脚本 1:1）",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "verify:ci": "node scripts/verify-ci.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "typescript": "^5.9.3",
    "vite": "^8.3.0",
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "echarts": "^6.1.0"
  }
}
