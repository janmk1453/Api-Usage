# API用量统计 — 从酒馆助手脚本迁移至原生扩展（RE 3.0）详尽迁移重构计划

> 版本：草案 v0.2（已确认选型） | 日期：2026-08-30 | 位置：`RE3.0/`
> 目标：将 `DeepSeek使用预测.js`（2752 行 IIFE，酒馆助手脚本）完整迁移至 SillyTavern 原生扩展 `API用量统计`，同步进行代码现代化、逻辑整理与 UI 全面重构。
> 已确认：扩展名 `API用量统计` / 打包 `Vite` / 样式 `无框架·SmartTheme` / 图表 `ECharts` / 存储 `分页（IndexedDB）` / 密钥 `XOR` / `minimum_client_version 1.11.0` / 独立仓库暂缓创建 / 峰值圆点保留 / WebDAV 保留 / 脚本主线不变 / UI 对标 `https://platform.deepseek.com/usage`。

---

## 目录

1. [现状盘点](#1-现状盘点)
2. [从脚本到扩展能带来哪些变化](#2-从脚本到扩展能带来哪些变化)
3. [总体目标与原则](#3-总体目标与原则)
4. [技术选型](#4-技术选型)
5. [新扩展目录结构](#5-新扩展目录结构)
6. [核心模块重构设计](#6-核心模块重构设计)
7. [UI/UX 全面重构方案](#7-uiux-全面重构方案)
8. [数据与兼容性迁移](#8-数据与兼容性迁移)
9. [关键难点与对策](#9-关键难点与对策)
10. [分阶段实施路线图](#10-分阶段实施路线图)
11. [质量保障与发布](#11-质量保障与发布)
12. [待确认问题](#12-待确认问题)
13. [附录](#13-附录)

---

## 1. 现状盘点

### 1.1 文件与规模

| 文件 | 作用 | 规模 |
|------|------|------|
| `DeepSeek使用预测.js` | 唯一真源，IIFE 单文件 | 2752 行，含 `PANEL_HTML` 内联字符串、全部业务逻辑 |
| `DeepSeek_Statistic_V2.36.json` | 酒馆助手可导入包，`content` 与 JS 同步 | 需手动同步 |
| `DeepSeek_Statistic_auto_update*.json` | 自动更新加载器 | 指向 gh-pages |
| `README.md` | 文档与 Changelog | 202 行 |

### 1.2 功能清单（按调用链归类）

**A. 数据拦截与计量**
- `patchFetch` 猴补 `window.parent.fetch`，拦截 `/api/backends/chat-completions/generate`，解析 `usage`（含 `prompt_cache_hit/miss`、流式拼接、备用 `prompt_tokens_details.cached_tokens`）
- `processUsage` → `calcCost`/`calcSavings` → 写 `state.saves[currentSave].history` + 聚合字段
- 支持流式/非流式、推理（`reasoning_tokens`/`thinkTime`）、`TTFT`/`tokenRate`/`duration`

**B. 定价与计费**
- `PRICING` 三模型内置价表（`offpeak/peak`），`DEFAULT_PEAK_HOURS` 09:00-12:00 / 14:00-18:00
- `isWeekendDay`/`isPeakHour`（北京时区周末全天低谷）、`_dsLocalDay`（+8h 自然日）、`mergePrices`/`getPricing`（自定义覆盖）
- `customModels` 模型级 `usePeakPricing` 开关、`peakHours` 多段（含跨天）、`useNewPricing` + `newPricingDate`、周末豁免、仅 DeepSeek 官方模型（`deepseek*` 前缀）享受峰谷价
- `recalcAllCosts` 历史重算，`hasPriceForModel` 未定价回退

**C. 存档与状态**
- `state` 单例：`saves/currentSave/lastUsage/panelOpen/chartPanelOpen/chartLibLoaded/chartModel/overviewModel/balance/customBalance/settings/messageCount`
- `saves[key]` 聚合：`total_tokens/total_cost/input_tokens/output_tokens/cache_*_tokens/input_cost/output_cost/rounds/history[≤500]`，`DETAIL_KEEP=10` 裁剪 `messages/fullRequest/fullResponse`
- `getSelectedSave`/`getMergedStats`（`__all__` 合并）、`createNewSave`（`YYYYMMDD_HHmmss_角色名`）、`deleteSave`

**D. 持久化**
- 双写：`getAllVariables/replaceVariables`（酒馆助手变量，`saveWithRetry` 3 次重试）+ `localStorage`（`saveToLS/loadFromLS`，前缀 `ds_`）
- `saveData/loadData`，失败 `error+toast`，`loadSavedData` 损坏备份 `ds_saves_corrupt_backup_<ts>`
- 键：`ds_api_key/ds_balance_data/ds_saves/ds_current_save/ds_settings/ds_message_count/ds_custom_balance/ds_last_version/ds_sync_meta/ds_webdav_pass/ds_peak_dot_pos`
- `syncMeta` LWW 时间戳，`touchSyncMeta`

**E. 余额与剩余轮次**
- `queryBalance/autoQueryBalance` 直连 `api.deepseek.com/user/balance`，`customBalance` 覆盖，`autoBalance+balanceInterval`
- `calculateRemainingRounds` EWMA(alpha=0.3) 预估单轮成本，仅计 `deepseek*`

**F. 导入导出**
- `exportHistory` 白名单导出（`format=deepseek-stat-export, version=1`），剔 `messages/fullRequest/fullResponse` 与 API Key
- `normalizeImportData` 版本检查、迁移链、缺省补全、`_dsIsUnsafeKey` 防原型污染
- `applyImportedData` `overwrite/merge`（timestamp 去重）、`recalcAllCosts`

**G. WebDAV 云同步**
- `buildLocalBundle/mergeBundles/applyMerged/webdavGet/webdavPut/webdavMkcol/webdavReqUrl`
- `pull-merge-push` 双向合并：`saves` 并集、`history` timestamp 去重（本地更完整覆盖）、标量按 `_mtime`、元数据按 `_ts` LWW
- 安全：强制 `https`、密码独立 `ds_webdav_pass` XOR 存储、仅 `https`、同步包白名单、支持 CORS 代理（ST `proxy?url=` 或 Worker 路径式）

**H. 图表与对比**
- `loadChartLib`（3 CDN + plugin-zoom 回退）、`createChartUI/toggleCharts`、`_chartInstances/_chartDayMode/_chartMonthIdx/_dayAggCache`
- 7 图：Token 趋势/费用趋势/命中率/请求次数/耗时+速率/模型占比 + 热力图 + Model Summary 表，支持 `轮次↔日期`、`Token↔次数`、月份选择、自定义滑块（`ds-slider-*`）、`zoom` 插件
- 消息对比：`_dsHandleCompare`（旧/新）+ Diff 染块、`showUsageDetail` 详情弹窗

**I. UI 与交互**
- `PANEL_HTML` 字符串拼接内联 CSS、`createUI/createChartUI/createSettingsUI/createPeakDot` 动态注入 `window.parent.document`
- 三层弹窗：主面板 / 统计详情 / 设置（含 `WEBDAV_PANEL_HTML/SETTINGS_PANEL_HTML`），均为 `position:fixed` + `overlay`，`--ds-vvh` 视口修复，`setupVisibilityFix/forcePanelRepaint` 合成层重建
- 导入/确认弹窗 `position:absolute` + `getBoundingClientRect` 文档偏移居中（规避 `transform` 祖先陷阱），`capture` 监听 `scroll/resize`
- 峰值小圆点：`getPeakDotStatus`（红/黄/绿）、可拖动记忆、仅主页面显示、`peakDot` 开关
- 扩展入口：`extensionsMenu` 注入 `ds_wand_entry`（魔杖菜单）、`ds-btn-check-update`

**J. 其他**
- 日志 ` _ds_log.debug/warn/error` + `getItem('ds_debug_log')`，`_ds_toast` 包 `toastr`
- `checkForUpdates` 拉 `gh-pages` 原始 JS 正则比对 `_ds_current_version`
- `setupEvents` 监听 `MESSAGE_RECEIVED` + 按钮事件、`generateDebugBatch` 批量假数据、`encryptKey/decryptKey` XOR+b64

### 1.3 现存问题（重构需解决）

- 单文件 2752 行，零模块化，`state` 全局可变，无类型，`var` + 回调地狱
- `PANEL_HTML` 字符串 CSS 难维护、转义易错，无响应式设计系统
- DOM 直接操作 `parent.document`，样式污染、`z-index` 硬编码（100000/200000/230000/999999）、`position:fixed` 在 `transform` 祖先下错位（已用 absolute hack 但仍脆弱）
- 持久化耦合酒馆助手 `getAllVariables`，未来需迁移
- `fetch` 猴补全局，易与其它脚本/扩展冲突，未利用 ST 官方 `GENERATION_*` 事件
- Chart.js 动态外链，多 CDN 不稳定，无打包
- 无构建、无类型、无测试、无 lint/format
- 峰值圆点、滑块等交互手写，未复用 ST 组件

---

## 2. 从脚本到扩展能带来哪些变化

> 本节为你专门回答“迁移能带来哪些变化”，按维度对比。

### 2.1 能力边界

| 维度 | 酒馆助手脚本（当前） | SillyTavern 原生扩展（迁移后） | 变化与收益 |
|------|----------------------|-------------------------------|------------|
| **加载与生命周期** | IIFE 注入 iframe，`setTimeout 2s` 猜就绪，无生命周期钩子，需 `appendInexistentScriptButtons` | `manifest.json` 声明 `js/css`，`hooks: {install,update,delete,enable,disable,activate,clean}`，`APP_INITIALIZED/APP_READY` 事件，`loading_order` 有序 | 生命周期可控，支持安装/卸载/启用/禁用/清理钩子，可做数据迁移与清理，不再靠定时猜 |
| **API 稳定性** | 依赖 `TavernHelper` 非官方 API（`getVariables/replaceVariables/eventOn/getButtonEvent`），`window.parent.fetch` 猴补 | 官方 `SillyTavern.getContext()`（`chat/characters/extensionSettings/chatMetadata/eventSource/event_types` 等），`SillyTavern.libs` 复用库 | 官方维护、文档化、向后兼容更好；减少未公开 API 破裂风险；一条 `getContext()` 取全量上下文 |
| **事件与拦截** | 猴补 `fetch` 截 `/api/backends/chat-completions/generate`，流式需手拼 `data: ` 行 | `eventSource.on(GENERATION_STARTED/STOPPED/ENDED, MESSAGE_RECEIVED, CHAT_CHANGED...)` + `generate_interceptor`（`manifest.generate_interceptor` 全局钩子，可改 `chat/contextSize/abort/type`） | 更可靠、与 ST 生成管线同频；可区分 `quiet/regenerate/swipe/impersonate`；不污染全局 `fetch`，减少冲突 |
| **持久化** | 双写 `getAllVariables` + `localStorage`，容量受限，需自管重试与损坏备份 | `extensionSettings[MODULE] + saveSettingsDebounced()`（服务端 `settings.json`，自动持久化），`chatMetadata` 绑定聊天，`character.data.extensions` 绑角色，`localforage`（IndexedDB）可选 | 服务端持久化、跨设备随账号；按扩展隔离命名空间；支持聊天级/角色级数据；容量远大于 LS |
| **UI 集成** | 手写 `overlay+panel` 注入 `parent.document.body`，`fixed/absolute` 手算居中，样式内联字符串 | `renderExtensionTemplateAsync('third-party/…','template')` Handlebars 模板 + DOMPurify + `data-i18n`，挂 `$('#extensions_settings2').append(...)` 官方设置区，`loader/Popup` 官方组件 | 风格与 ST 原生一致，自动净化与 i18n；无需手写居中/hack；`Popup.show.confirm/input/text` 与 `loader.show` 统一体验 |
| **样式与依赖** | `PANEL_HTML` 内联 CSS，`Chart.js` CDN 外链，无打包 | `manifest.css` + 打包（`Extension-WebpackTemplate` 支持 `npm`，可引入 `vue/react/tailwind` 等），`SillyTavern.libs` 复用 `lodash/moment/Handlebars/DOMPurify` 等 | 可用现代框架与包管理，样式隔离、tree-shaking；不依赖外网 CDN；与 ST 共享库减体积 |
| **分发与更新** | 手动导入 JSON，`gh-pages` 轮询 `raw` 拉新版字符串比对，`jsDelivr` 缓存延迟高 | `manifest.auto_update=true`，ST 在包更新时自动拉取；可发布至 [SillyTavern-Content](https://github.com/SillyTavern/SillyTavern-Content) 官方仓库 | 用户在“管理扩展”一键安装/更新；无需手动导 JSON；版本由 `manifest.version` 统一 |
| **安全与隔离** | 明文 `localStorage`，XOR 仅混淆，`fetch` 猴补影响全局 | `extensionSettings` 仍明文（官方提示勿存密钥，但有服务端存储），密码仍需本地加密；扩展按文件夹隔离，依赖声明 `dependencies` | 隔离性更好，但密钥仍需谨慎（官方建议用 server plugin 处理敏感信息） |
| **国际化** | 硬编码中文，无 i18n | `manifest.i18n` + `addLocaleData` + 模板 `data-i18n`/`t` | 可多语言，PR 更易被官方收录 |
| **可维护性** | 零类型、零构建、单文件 | `global.d.ts`（`import '../../../../public/global'` 类型补全）+ `ts/typecheck` + 可选测试 | 可自动化、类型安全、团队协作友好 |

### 2.2 对用户的直观变化

- **安装**：从“下载 JSON → 酒馆助手导入”变为“扩展管理 → 一键安装/更新”，零手动同步。
- **设置入口**：从魔杖菜单注入变为 ST 侧边栏“扩展设置”原生折叠面板，与其他扩展一致。
- **稳定性**：不再猴补 `fetch`，与其它脚本/扩展冲突率大幅降低；生成事件与 ST 同生命周期，统计更准。
- **数据**：存档随 ST `settings.json` 服务端保存，换浏览器/设备仍可用；支持按聊天/角色绑定（未来可按角色卡分存档）。
- **性能**：打包后单次加载，无多 CDN 失败重试；`localforage` 异步不阻塞主线程。
- **外观**：统一 ST 设计语言（`inline-drawer` 等），深色主题自适应，移动端抽屉式，不再有 `z-index` 遮挡与错位。

### 2.3 对开发者的变化

- 可用 `npm` + `TypeScript` + `ESM` + `Vite/Webpack`，模块化拆分 2.7k 行；
- 可写 `manifest.hooks` 做版本迁移，`onUpdate` 中自动搬运旧 `localStorage` 数据；
- 可用 `Popup/loader/macros/generateRaw/generateQuietPrompt/function calling` 等扩展专属能力，未来可加斜杠命令 `/ds-stats`、宏 `{{ds-cost}}`、函数工具等。

> **参考项目 `cone97218-alt/ds` 定位**：该项目为早期非官方迁移，仅作“扩展如何组织 `manifest.json/index.js/style.css`”的形态参考，不作为功能与 UI 的实现依据；本计划不复刻其逻辑，仅借鉴其目录与清单字段。

---

## 3. 总体目标与原则

### 3.1 目标

1. **完整平移**：现脚本全部能力 1:1 迁移（定价/存档/余额/导入导出/WebDAV/图表/对比/峰值圆点/调试等），无功能回退。
2. **现代化**：`TypeScript + ESM + 模块化`，`manifest + 打包`，移除 IIFE 与全局 `var`。
3. **美观统一高效**：重写 UI 为 ST 原生风格，深色毛玻璃、卡片化、响应式抽屉，动效节制，首屏无外网阻塞。
4. **可维护**：强类型、分层、单向数据流、持久化抽象、可测试。

### 3.2 原则

- **数据不丢**：`onInstall/onUpdate` 首次启动自动迁移旧 `localStorage/TavernHelper` 数据，写备份 `ds_migration_backup_<ts>`。
- **渐进增强**：先可用再精致，分阶段交付，每阶段可独立发版。
- **官方优先**：凡 ST 已提供（事件/存储/模板/组件），一律优先于自造。
- **零全局污染**：不猴补 `fetch`，不直接改 `parent.document.body` 样式前缀外泄，扩展样式以 `[data-extension="ds-stat"]` 作用域隔离。
- **可回滚**：保留“导出旧数据”按钮，迁移失败 `Popup.confirm` 提示并回滚。

---

## 4. 技术选型

| 层 | 选型 | 理由 |
|----|------|------|
| **语言** | TypeScript (strict) | 现脚本无类型，重构需类型保障定价/存档结构的正确性 |
| **打包** | **Vite**（`lib` 模式，`cssCodeSplit:false`） | 已确认 Vite，产出 `dist/index.js + style.css`，速度与简洁优于 Webpack |
| **模板** | Handlebars (`renderExtensionTemplateAsync`) | 官方推荐，自动净化，无需引入 Vue/React 即可贴合 ST |
| **图表** | **ECharts 5** 本地 `npm` 依赖，打包进产物 | 已确认 ECharts，替代 Chart.js；热力图/大数据/主题定制更强，包体以 `tree-shaking` + 按需 `import * as echarts from 'echarts/core'` 控制 |
| **样式** | **无框架 + SmartTheme**，`style.css` 结合 DeepSeek 官方 `platform.deepseek.com/usage` 视觉（见 §7 增补） | 已确认沿用 `ds-main` SmartTheme 体系，叠加 DeepSeek 官方卡片/表格/数据可视化风格，不引入 Tailwind/Vue |
| **状态** | 轻量 `store`（`zustand`/`nanostore` 或自实现 `EventTarget` + `structuredClone`） | 替代全局 `state` 可变单例，支持订阅与持久化节流 |
| **存储** | **分页方案**：`extensionSettings[MODULE]` 存热数据 50 条 + 聚合，`localforage`（IndexedDB）存全量历史，`maxHistoryItems=5000` 可配 | 已确认分页，参考 `ds-main` 的 `deepseek_stats_db` + `maxHistoryItems` 思路 |
| **密钥** | **XOR+b64**（`XOR_KEY='ds-stats-v1-xor-key!@#$%^&*'`）保持兼容 | 已确认不升级 AES-GCM，零迁移成本，与现脚本/ds-main 一致 |
| **工具** | `SillyTavern.libs.lodash/moment/DOMPurify` 复用；`zod@4` 做存档/导入 schema 校验 | 复用 ST 已加载库减包体；`zod` 保证导入容错 |
| **i18n** | `manifest.i18n` + `addLocaleData` | 为后续英文化预留 |
| **最低版本** | `minimum_client_version: "1.11.0"` | 已确认 1.11.0 |

> **不引入**：`Vue/React`（已确认无框架）、`AES-GCM`（已确认 XOR）、`Webpack`（已确认 Vite）；独立仓库暂缓创建，`RE3.0` 先本地开发，脚本主线不变。

---

## 5. 新扩展目录结构

```
RE3.0/
├── 迁移重构计划.md              # 本文
├── manifest.json                # 扩展清单（display_name/loading_order/js/css/i18n/hooks 等）
├── index.js                     # 入口（ESM，导出 hooks，注册事件/拦截器/设置UI）
├── style.css                    # 扩展样式（作用域隔离，响应式抽屉，毛玻璃卡片）
├── global.d.ts                  # ST 全局类型补全（import '../../../../public/global'）
├── package.json                 # npm 依赖与脚本（build/dev/typecheck）
├── vite.config.ts               # 或 webpack.config.js
├── tsconfig.json
├── README.md                    # 扩展版文档（安装/使用/迁移说明）
├── LICENSE
├── i18n/
│   ├── zh-cn.json
│   └── en-us.json
├── templates/
│   ├── settings.html            # 扩展设置面板（inline-drawer 结构，Handlebars 变量）
│   ├── panel.html               # 主统计面板（可复用 Popup 或 inline-drawer 抽屉）
│   ├── charts.html              # 统计详情（canvas 容器 + 滑块 + 热力图）
│   └── usage.html               # 单条详情弹窗
├── src/
│   ├── index.ts                 # 入口逻辑（activate + 事件绑定）
│   ├── hooks.ts                 # 导出 onInstall/onUpdate/onDelete/... 钩子
│   ├── constants/
│   │   ├── pricing.ts           # PRICING, DEFAULT_PEAK_HOURS, MAX_HISTORY, DETAIL_KEEP
│   │   └── storage-keys.ts      # 存储键与版本常量
│   ├── store/
│   │   ├── index.ts             # 状态容器（saves/currentSave/balance/settings 等）
│   │   ├── selectors.ts         # getSelectedSave/getMergedStats/calculateRemainingRounds
│   │   └── persistence.ts       # load/save/migrate（extensionSettings + localforage 适配）
│   ├── services/
│   │   ├── pricing.ts           # isWeekendDay/isPeakHour/getPricing/calcCost/calcSavings/recalcAllCosts
│   │   ├── saves.ts             # createNewSave/deleteSave/refreshSaveSelect 业务
│   │   ├── balance.ts           # queryBalance/autoQueryBalance（带节流与错误分级）
│   │   ├── import-export.ts     # exportHistory/normalizeImportData/applyImportedData
│   │   ├── sync.ts              # WebDAV pull-merge-push（webdavGet/Put/Mkcol/auth/proxy）
│   │   ├── interception.ts      # GENERATION_* 事件监听 + usage 提取（替代 patchFetch）
│   │   └── debug.ts             # generateDebugBatch
│   ├── utils/
│   │   ├── date.ts              # _dsLocalDay、北京时区工具
│   │   ├── crypto.ts            # XOR+b64（过渡）→ 评估 Web Crypto 替代
│   │   ├── logger.ts            # _ds_log 分级 + 去重 + toast 包装
│   │   ├── sanitize.ts          # _ds_esc、_dsIsUnsafeKey
│   │   └── viewport.ts          # --ds-vvh、峰值圆点定位、visibilityFix
│   ├── ui/
│   │   ├── panel.ts             # 主面板创建/开关/刷新（createUI/togglePanel/refreshUI）
│   │   ├── charts.ts            # 图表面板（createChartUI/renderCharts + 滑块/月份/模型切换）
│   │   ├── settings.ts          # 设置面板绑定（bindSettingsControls/refreshSettingsUI/pricing editors）
│   │   ├── compare.ts           # 消息对比与 diff 渲染
│   │   ├── usage-detail.ts      # 单条 usage 详情
│   │   ├── peak-dot.ts          # 峰值圆点（createPeakDot/updatePeakDot/drag）
│   │   └── components/
│   │       ├── model-editor.ts  # modelEditorRowHtml/readCustomModelRow 等
│   │       └── peak-hours-editor.ts
│   └── types/
│       ├── save.ts              # Save/HistoryEntry/Usage 等
│       └── settings.ts          # Settings/WebDAV/Balance 等
└── dist/                        # 构建产物（gitignore，仅发版时提交至 gh-pages/extension 分支）
```

**manifest.json 要点（示例）**

```json
{
  "display_name": "API用量统计",
  "loading_order": 20,
  "js": "index.js",
  "css": "style.css",
  "author": "janmk1453",
  "version": "3.0.0",
  "homePage": "",
  "auto_update": true,
  "minimum_client_version": "1.11.0",
  "i18n": { "zh-cn": "i18n/zh-cn.json" },
  "hooks": {
    "install": "onInstall",
    "update": "onUpdate",
    "delete": "onDelete",
    "enable": "onEnable",
    "disable": "onDisable",
    "activate": "onActivate"
  }
}
```
> 已按确认更新：`display_name=API用量统计`，`loading_order` 对齐 `ds-main` 的 20，`minimum_client_version=1.11.0`，`homePage` 暂空（独立仓库待建后回填）。

---

## 6. 核心模块重构设计

### 6.1 存储抽象（最高优先级）

**现状**：`saveWithRetry/loadWithRetry`（TavernHelper）+ `saveToLS/loadFromLS` 双写，键分散，全局 `state` 直接可变。

**重构**：

```ts
// src/store/persistence.ts
const MODULE = 'ds_stat'; // extensionSettings 命名空间

export type Persisted = {
  saves: Record<string, Save>;
  currentSave: string | '__all__' | null;
  balance: Balance | null;
  customBalance: string | null;
  settings: Settings;
  messageCount: number;
  syncMeta: SyncMeta;
};

export async function load(): Promise<Persisted> { /* 1) extensionSettings[MODULE] 2) 回退 localStorage 迁移 */ }
export function save(patch: Partial<Persisted>): void { /* 合并 + saveSettingsDebounced() 节流 300ms */ }
export async function migrateIfNeeded(): Promise<void> { /* 检测旧键 ds_saves 等，备份后写入新位 */ }
```

- 首次 `onInstall` 调用 `migrateIfNeeded`，将 `ds_saves/ds_settings/...` 搬至 `extensionSettings[MODULE]`，写 `ds_migration_backup_<ts>` 至 `localforage`。
- 历史 `history` 超 `MAX_HISTORY` 时转 `localforage` 分页，`getSelectedSave` 合并时懒加载。

### 6.2 拦截重构（去猴补）

**现状**：`patchFetch` 包 `parent.fetch`，手拆 `usage`。

**重构**：

- 监听 `eventSource.on(event_types.GENERATION_ENDED, ...)` 与 `MESSAGE_RECEIVED`，从 `SillyTavern.getContext().chat` 尾条取 `extra.api_usage` / `extra.token_count`（ST 已标准化），回退解析 `fetch` 响应（仅作兜底，且用 `eventSource` 而非全局猴补）。
- 若需改写请求，用 `manifest.generate_interceptor = "dsGenerateInterceptor"` + `globalThis.dsGenerateInterceptor = (chat, ctxSize, abort, type) => { ... }`，而非 monkey patch。
- `processUsage` 保持纯函数：`Usage → HistoryEntry`，`calcCost` 依赖注入 `settings/pricing`。

### 6.3 定价引擎

- 抽 `src/services/pricing.ts`，全函数纯化，`isPeakHour(timestamp, peakHours)` 不再读全局 `state`，便于单测。
- `PRICING` 转 `Map<string, Pricing>`，`getPricing(model, customModels)` 明确回退链：自定义 > 内置 > `flash`。
- 保留周末豁免与 `isDeepSeekOfficialModel` 过滤。

### 6.4 导入导出与同步

- `normalizeImportData` 用 `zod` 重写：`ExportSchema = z.object({ format: z.literal('deepseek-stat-export'), version: z.number(), data: PersistedSchema })`，未知字段 `passthrough` 保留。
- `mergeBundles` 保持双向合并语义，提为 `src/services/sync.ts` 纯函数，单测覆盖 `timestamp` 去重与 `_ts` LWW。
- WebDAV 密码改 `localforage` + `Web Crypto API`（AES-GCM）存储，`extensionSettings` 仅存 `hasWebdavPass: boolean`；兼容旧 `ds_webdav_pass` 解密迁移。

### 6.5 日志与错误分级

- `src/utils/logger.ts` 复用现 `_ds_log` 语义：`debug`（`localStorage ds_debug_log=1` 开关）/`warn` 去重/`error` 必显；`toast` 统一 `toastr` 包装。
- 关键路径（`save/load/mergeBundles/processUsage`）按 AGENTS.md 矩阵：数据丢失→`error+toast+备份`，降级→`warn`，可选副作用→`debug`。

---

## 7. UI/UX 全面重构方案

### 7.1 设计目标

- **现代**：ST 原生 `inline-drawer` + 卡片 + 细边框 + 柔和阴影，深色毛玻璃（`backdrop-filter: blur`）与 ST 主题变量一致。
- **简洁**：信息分层，首屏仅“余额/剩余轮次/总览/存档/历史”，图表与设置下沉为二级抽屉。
- **统一**：所有弹窗复用 `Popup` 或同一样式抽屉，`fixed` 居中由 `Popup` 接管，不再手算 `getBoundingClientRect`。
- **高效**：无外链阻塞，首屏 <100ms 渲染；ECharts 懒加载；移动端全屏抽屉，手势友好。
- **DeepSeek 官方对标**：整体对齐 `https://platform.deepseek.com/usage`（见 §7.5 新增），采用其极简数据仪表盘语言：大数字 + 轻表格 + 克制色彩 + 留白。
- **内容不缩水**：样式对齐 DeepSeek，但展示内容点与丰富度与脚本完全一致（全部统计卡、7 图、热力图、模型汇总、历史、对比、峰值、WebDAV 等均保留，不因对标而删减）。

### 7.5 DeepSeek 官方 `platform.deepseek.com/usage` 风格对标（基于截图精确提取）

> 已基于你提供的 `https://platform.deepseek.com/usage` 截图精确取色，非推测。

**截图实测色板**
- 页面底色：`#FFFFFF` 纯白；侧边栏：`#F9FAFB` 极浅灰；卡片：`#F6F7F8` / `#F5F5F7` 浅灰（`background: #F6F7F8; border: none; border-radius: 12-16px; padding: 16-20px`），无描边，靠留白与圆角区分层级。
- 文字：主标题/数值 ` #111827` 近黑（`font-weight: 600-700`），`CNY` 单位/说明 ` #9CA3AF` / `#6B7280` 灰，`≈ 30%` 透明度做次级信息。
- 强调：黑色 pill 按钮 `#111827` 白字 `#FFFFFF`（`去充值/导出`），`border-radius: 999px; padding: 8px 18px`。
- 状态标签：`余额预警已开启 去设置` 为薄荷绿 `#E6F8EC` 底 + `#0BA25E` 字，圆角 6px。
- 图表：柱状 ` #FF6A00` / `#FF7A00` 橙（顶部 4px 圆角 `borderRadius: [4,4,0,0]`），`y` 轴 `#9CA3AF` 11px，`x` 轴日期 `8/1 8/11…`，网格线 `#E5E7EB` 1px 淡线，`barWidth: 12-14`。
- 边框与分隔：`1px solid #E5E7EB` 极淡分隔线，`12px` 留白体系（`gap: 12-16px`）。

**落地策略（与 ST 共存）**
- 隔离容器：扩展主面板根节点设 `data-ds-theme="light"`，内部强制 `background: #FFFFFF; color: #111827`，不跟随 `SmartTheme` 深色，避免 ST 深色主题把浅灰卡片染黑；仍用 `SmartTheme` 变量作降级回退（检测 `prefers-color-scheme` 时可切深色映射：卡片 `#1F2937` / 文字 `#F9FAFB`）。
- 组件映射：
  - 卡片 `.ds-card`：`background: #F6F7F8; border-radius: 14px;`（非 `BlurTintColor`），标题 `12-13px/500/#6B7280`，数值 `26-28px/700/#111827` + 单位 `14px/400/#9CA3AF`。
  - 按钮 `.ds-btn-primary`：`background: #111827; color: #fff; border-radius: 999px`；次级 `.ds-btn-normal`：`background: #F6F7F8; color: #111827`。
  - 表格/历史列表：表头 `11px/500/#9CA3AF uppercase`，行 `hover: #F6F7F8`，与截图的明细表一致。
  - 图表 ECharts：`color: ['#FF6A00']` 橙为主（费用柱），`grid: {left: 32, right: 12, top: 16, bottom: 28}`，`axisLine: #E5E7EB`，`textStyle: #6B7280`，精确复刻截图柱状。
- 字体：`'Inter', system-ui, -apple-system, sans-serif`，数值 `font-variant-numeric: tabular-nums`，与 DeepSeek 官方一致。

**信息架构（截图层级 + 脚本内容 1:1）**
- 原则：**截图定层级与视觉，脚本定内容体量**。不用截图的 3-4 卡收敛替代脚本的 20+ 指标。
- 顶部：`用量信息` 标题 + 副说明 `所有日期均按 GMT+8 时间显示…`（截图第一行）。
- 第一行：`充值余额 ¥4.78 CNY + 去充值`（左） / `累计消费金额 ¥20.21 CNY`（右）—— 双浅灰卡 1:1（脚本的 `余额+剩余轮次+自定义余额` 融合于此，花色保持 DeepSeek）。
- 第二行：筛选条 `时间维度 近30天 / API Key 全部 / 模型 全部` + `导出/导入` 黑 pill + `存档 下拉 + 新建/删除/清空`（脚本存档与导入导出能力以 pill/下拉形式融入筛选条，不另起深色区块）。
- 第三行：**三主卡** `消费金额 ¥4.06 / API请求次数 537 / Tokens 26,090,981`（截图三卡，作首屏主指标）。
- 第四行起：**脚本全部统计卡 1:1 保留**，按 DeepSeek 浅灰卡平铺：`总消耗/加权命中率/平均每轮/预计节省/输入费用/输出费用/总Tokens/命中Tokens/未命中Tokens/轮数/单轮最大/单轮平均/最新命中率/命中未命中比/单轮平均输入/输出/节省比例/单轮最小/单轮最大Tokens/单轮最小Tokens/平均耗时/平均速率/思维链`（约 23 卡，`grid-template-columns: repeat(3,1fr)`，移动端 2 列，可折叠分组“更多指标”但默认展开，确保丰富度一致；`ds-main` 的 `statsVisibility` 显隐保留）。
- 中段：`消费金额 (CNY) ¥4.06` 橙柱总图（ECharts 柱状，截图同款） + 上方 `模型/API Key` 胶囊切换；下方展开 7 图（脚本的 Token趋势/费用趋势/命中率/请求次数/耗时速率/模型占比 + 热力图 + Model Summary 表）全部以 DeepSeek 浅灰卡包裹，ECharts 橙/灰配色，不删减。
- 底部：`历史记录` 浅灰表（脚本的 `时间/模型/Token/费用/耗时/速率/旧|新|详情` 完整列） + `缓存断点` 对比（Diff 染块） + `最新一条` 卡，全部以浅灰卡 + 极淡分隔线呈现，交互与脚本一致。
- 本扩展层级 = 截图的极简骨架 × 脚本的完整血肉，视觉是 DeepSeek，数据是脚本。

### 7.2 布局（样式=DeepSeek，内容=脚本 1:1）

```
扩展主面板（data-ds-theme="light" 强制浅色隔离容器，#FFFFFF 底）
├── 标题区：用量信息 + 副说明（GMT+8 / 5分钟延迟，截图同款）
├── 第一行 双卡：充值余额（¥ + 去充值 pill + 余额预警薄荷标签） | 累计消费金额（脚本余额+剩余轮次 EWMA 融合）
├── 筛选条：时间维度 近30天 / API Key 全部 / 模型 全部 + 存档下拉 + 新建/删除/清空 + 导出/导入 pill（脚本存档与导入导出融入）
├── 三主卡：消费金额 / API请求次数 / Tokens（截图三卡，首屏主指标）
├── 全部统计卡（脚本 23 卡 1:1 保留，浅灰卡 grid 3列）：总消耗/加权命中率/平均每轮/预计节省/输入费用/输出费用/总Tokens/命中/未命中/轮数/单轮最大/平均/最新命中率/命中未命中比/平均输入/输出/节省比例/单轮最小/最大最小Tokens/平均耗时/速率/思维链（支持 statsVisibility 显隐与折叠分组，默认全展开保丰富度）
├── 总柱状：消费金额 (CNY) 橙柱（ECharts，截图同款，y 0-2.4，x 8/1 8/11…） + 模型/API Key 胶囊
├── 7 图 + 热力图 + Model Summary（脚本图表 1:1 保留，ECharts 浅灰卡包裹，轮次/日期切换、月份选择、滑块、Zoom 均保留）
├── 历史记录（浅灰表，时间/模型/Token/费用/耗时/速率/旧|新|详情 完整列，虚拟滚动/分页）
├── 缓存断点对比（Diff 染块，旧/新选择，脚本一致）
├── 最新一条（脚本一致）
└── 悬浮区：峰值圆点（保留可拖动，§7.4）、刷新/清空

设置抽屉（浅灰卡分组，截图圆角 14px）
├── API 密钥 · 余额（自动校准/间隔/自定义余额，截图卡样式）
├── 定价（新价格开关/生效日期/模型价格卡/峰谷时段，脚本 pricing 1:1）
├── 调试（hit/miss/output/model/date/batchCount + 生成，脚本一致）
├── 峰值圆点（开关/重置位置）
└── WebDAV（url/user/pass/path/proxy + 立即同步 + CORS 折叠说明）

> 布局 = 截图骨架 × 脚本血肉；样式变量按 §7.3 截图取色，内容体量按脚本不删减。
```

### 7.3 样式规范（已确认：截图精确取色 + 双主题隔离）

- 作用域：`[data-extension="api-usage-stat"][data-ds-theme="light"]` 强制浅色，避免 ST 深色污染；容器内 `* { box-sizing: border-box; font-family: 'Inter', system-ui, -apple-system, sans-serif; }`。
- 变量（截图精确）：
  ```css
  --ds-bg: #FFFFFF; --ds-sidebar: #F9FAFB; --ds-card: #F6F7F8; --ds-card-hover: #EEF0F2;
  --ds-text: #111827; --ds-text-2: #6B7280; --ds-text-3: #9CA3AF; --ds-border: #E5E7EB;
  --ds-black: #111827; --ds-orange: #FF6A00; --ds-green-bg: #E6F8EC; --ds-green: #0BA25E;
  --ds-radius-card: 14px; --ds-radius-pill: 999px; --ds-gap: 12px;
  ```
  深色回退：`@media (prefers-color-scheme: dark) [data-ds-theme="auto"] { --ds-card: #1F2937; --ds-text: #F9FAFB; … }`。
- 组件：主按钮 `background: var(--ds-black); color: #fff; border-radius: var(--ds-radius-pill)`；卡片 `background: var(--ds-card); border-radius: var(--ds-radius-card); padding: 16px 20px`；数值 `font-size: 26px; font-weight: 700; font-variant-numeric: tabular-nums`。
- 动效：`transition: background 0.15s ease, transform 0.2s ease, opacity 0.2s ease`，`will-change` 仅开合时加。
- 响应式：`@media (max-width: 760px)` 抽屉全屏，`grid-template-columns: repeat(3,1fr)` 在 760px 以下 `repeat(1,1fr)`，与截图三卡一致。
- 图表：ECharts `import * as echarts from 'echarts/core'` + `BarChart/Grid/Tooltip/DataZoom`，`color: ['#FF6A00']`，`barWidth: 12`，`itemStyle.borderRadius=[4,4,0,0]`，精确复刻截图橙柱。

### 7.4 交互优化

- 峰值圆点：保留拖动记忆，但改 `position:fixed` + `Popup` 层级管理，`isTavernMainPage` 判断保留。
- 滑块：复用 `chartjs-plugin-zoom` + 自制 `ds-slider-*` 的拖拽改为 `Pointer Events`，触摸更顺。
- 对比：Diff 渲染抽 `src/ui/compare.ts`，用 `SillyTavern.libs.DiffMatchPatch` 替代手写。
- 空状态与加载：图表首开显示 `loader.show({message})`，失败 `Popup.show.text` 提示。

---

## 8. 数据与兼容性迁移

1. **检测**：`onInstall/onUpdate` 中检查 `extensionSettings[MODULE]` 是否为空且 `localStorage` 存在 `ds_saves`。
2. **备份**：`localforage.setItem('ds_migration_backup_'+Date.now(), JSON.stringify(old))`。
3. **搬运**：`JSON.parse(loadFromLS/loadWithRetry)` → `Persisted` → `extensionSettings[MODULE]=…; saveSettingsDebounced()`。
4. **校验**：`zod` 解析，损坏条目跳过并 `warn`，整体损坏则 `error+Popup` 提示手动导入。
5. **清理**：迁移成功后保留旧 LS 7 天，设置中提供“清理旧数据”按钮（`onClean` 钩子亦处理）。
6. **导出兼容**：保持 `deepseek-stat-export v1` 格式不变，老文件可直接导入；`WEBDAV_REMOTE_VERSION` 亦保持 `1`，仅扩展内部存储位置变。

---

## 9. 关键难点与对策

| 难点 | 对策 |
|------|------|
| 去 `fetch` 猴补后如何稳定拿 `usage` | 主路径 `GENERATION_ENDED` → `chat[tail].extra`，ST 已聚合 `usage`；辅路径保留轻量 `generate_interceptor` 记录请求 `messages`（不改 `fetch`） |
| 500 条历史 + Detail 裁剪的存储压力 | `extensionSettings` 存聚合 + 近期 50 条，超量分页至 `localforage`，`getMergedStats` 懒加载 |
| Chart.js 打包体积 | `chart.js/auto` 按需 `import { Chart, registerables }` + `chartjs-plugin-zoom`，`vite`  `manualChunks` 分离，首屏不加载图表 chunk |
| 峰值圆点与多弹窗层级 | 统一 `z-index` 阶梯：ST 主内容 < 峰值圆点(3000) < 设置抽屉(230000) < 图表(200000) < Popup(400000)，用 CSS 变量管理 |
| 旧用户无感升级 | `onUpdate` 幂等迁移，`Popup.confirm` 告知“已自动迁移，旧数据已备份”，提供“查看备份/回滚” |
| WebDAV CORS | 保留 `proxy` 配置与文档，扩展内 `fetch` 改走 `SillyTavern.getContext().doExtrasFetch` 或保持 `_dsRawFetch` 直连 + 代理，用户无需改动 |

---

## 10. 分阶段实施路线图

### 阶段 0 — 准备（1-2 天） ✅ 已完成 2026-08-30

- [x] 在 `RE3.0/` 初始化 `package.json/vite.config.ts/tsconfig.json/global.d.ts/manifest.json/style.css/index.js` 空壳，跑通 `npm run build` 产出可被 ST 加载的空扩展（显示 `inline-drawer` 占位）
  - 产物：`RE3.0/index.js` 3.13kB / `style.css` 2.3kB，`node --check` 与 `tsc --noEmit` 均通过，`manifest: API用量统计@3.0.0 / 1.11.0`
  - 样式：截图标定 `#FFFFFF/#F6F7F8/#111827/#FF6A00/#E6F8EC` 已入 `style.css`，`data-ds-theme="light"` 隔离
  - 入口：`src/index.ts` 导出 `onInstall/onUpdate/onActivate` 等 hooks，占位 `inline-drawer` 三卡（消费金额/API 次数/Tokens）
- [x] 确认本计划，回答 §12 待确认问题（已收敛为 v0.4）

### 阶段 1 — 核心平移（MVP，可用）（5-7 天）

- [ ] 迁移 `constants/pricing/store/persistence/services/pricing/saves`，跑通 `extensionSettings` 存取与旧数据迁移
- [ ] 迁移 `interception`（`GENERATION_ENDED`）+ `processUsage/calcCost/recalcAllCosts`，验证真实对话能记录
- [ ] 迁移 `balance` + `calculateRemainingRounds` + `isPeakHour/isWeekendDay`
- [ ] 用 `templates/settings.html + panel.html` 实现最小可用面板（概览/存档/历史/导入导出），样式先贴合 ST 原生

### 阶段 2 — 完整功能（1-2 周）

- [ ] 导入导出 + `zod` 校验 + `Popup` 确认
- [ ] WebDAV 同步（含 `proxy` 与 LWW 合并单测）
- [ ] 设置面板（定价编辑器/峰谷时段/调试/峰值圆点）
- [ ] 峰值圆点与 `visibilityFix/viewport` 迁移

### 阶段 3 — 图表与对比（1 周）

- [ ] 打包 `chart.js`，实现 `charts.html` + `src/ui/charts.ts`（7 图 + 热力图 + 滑块 + 月份导航 + 模型切换）
- [ ] 消息对比 `DiffMatchPatch` + `usage-detail` 弹窗
- [ ] 虚拟滚动优化历史列表

### 阶段 4 — 抛光与发布（3-5 天）

- [ ] i18n、空状态、加载态、错误态统一
- [ ] `manifest.hooks`（`onClean` 清理、`onDelete` 备份提示）
- [ ] 文档 `RE3.0/README.md` + 迁移指南 + 截图
- [ ] 自测清单：新装/升级/导入老文件/WebDAV 双向/峰谷计费/图表缩放/移动端
- [ ] 发版 `v3.0.0`，提交至 `SillyTavern-Content`（可选）

> 总计约 4-6 周，阶段 1 结束即可内测。

---

## 11. 质量保障与发布

- **类型检查**：`npm run typecheck`（`tsc --noEmit`）门禁
- **单测**：`vitest` 覆盖 `pricing/isPeakHour/calcCost/mergeBundles/normalizeImportData/calculateRemainingRounds`
- **手动验证**：`npm run dev` + ST `extensions/third-party` 软链本地调试；`APP_READY` 后检查 `extensionSettings[MODULE]` 写入
- **构建校验**：`npm run build && node --check dist/index.js`
- **发布**：`manifest.version` 单一真源，`RE3.0/README.md` Changelog 同步；`gh release create v3.0.0` 产出扩展压缩包（不再发 `DeepSeek_Statistic_V*.json`）

---

## 12. 待确认问题

> 请逐条确认，将直接影响阶段 0 的选型与目录。

1. **扩展名称与 ID**：是否沿用 `DeepSeek 使用预测`？`manifest.display_name` 与 `extensionSettings` 命名空间（建议 `ds_stat`）请确认；是否需英文名（`DeepSeek Usage Forecast`）以便提交官方仓库？
2. **打包工具偏好**：`Vite`（更快）还是 `Webpack`（官方模板默认）？本计划默认 `Vite`，若你更倾向与官方模板一致可切 `Webpack`。
3. **UI 框架**：首版是否坚持“无 Vue/React、仅 Handlebars + 原生 CSS”（最贴合 ST、包最小）？若希望更现代交互（如虚拟滚动、复杂表单），是否接受引入 `Vue 3` 或 `Preact`？
4. **图表方案**：是否保留 `Chart.js`？若追求更强热力图与大数据，是否考虑 `ECharts`？（会增体积）
5. **存储策略**：`500` 条历史全放 `extensionSettings`（简单但可能触 `settings.json` 体积告警）还是分页至 `localforage`（复杂但更稳）？默认后者，请确认阈值（建议 `50` 热数据 + 其余冷分页）。
6. **密钥存储**：现 `XOR+b64` 仅混淆，扩展版是否升级为 `Web Crypto AES-GCM`（需用户授权 `crypto.subtle`），或保持兼容以免迁移复杂度？（建议新版用 `AES-GCM` 并自动迁移旧密钥）
7. **峰值圆点**：是否保留可拖动小圆点？部分用户反馈遮挡，若保留是否默认关闭或改为状态栏图标？
8. **WebDAV 兼容**：是否保留现有 `WebDAV` 协议与文件名 `DeepSeekStatSync.json`？若改存储位置，需考虑老用户双端同步的平滑过渡。
9. **发布渠道**：`RE3.0` 是否作为独立仓库/分支发布，还是与当前 `deepseek-tavern-script` 同仓以 `extensions/` 子目录发布？这决定 `manifest.homePage` 与 `auto_update` 路径。
10. **参考项目 `cone97218-alt/ds`**：该项目 `index.js/manifest.json/style.css` 你是否有本地副本可提供？当前外网无法拉取，若能提供可进一步对齐其踩坑点（尤其是 `global.d.ts` 路径与事件取 `usage` 的细节）。
11. **最低 ST 版本**：`manifest.minimum_client_version` 设为 `1.12.0` 还是更低以兼容更多用户？（影响 `getContext` API 可用性）
12. **是否需要保留脚本版**：迁移后是否同时维护脚本版（`DeepSeek使用预测.js`）一段时间，或 `RE3.0` 直接成为主线、脚本版冻结？

---

## 13. 附录

### A. 官方文档关键摘录

- **清单**：`display_name/loading_order/js/css/author/version/homePage/auto_update/minimum_client_version/i18n/hooks/dependencies/generate_interceptor`
- **初始化**：`activate` 钩子做同步阻塞初始化；`APP_INITIALIZED` 做 UI 挂载；`APP_READY` 做异步重活
- **渲染**：`await renderExtensionTemplateAsync('third-party/<folder>', '<template>', data)` → `$('#extensions_settings2').append(html)`
- **存储**：`extensionSettings[MODULE] + saveSettingsDebounced()`；`chatMetadata + saveMetadata()`；`character.data.extensions`
- **事件**：`eventSource.on(event_types.*)`；`GENERATION_AFTER_COMMANDS/STARTED/STOPPED/ENDED`；`CHAT_CHANGED/CHARACTER_EDITED` 等
- **拦截器**：`manifest.generate_interceptor = "globalFn"`，`globalThis.globalFn = async (chat, contextSize, abort, type) => {}`
- **组件**：`loader.show({message, blocking, toastMode})/hide()`；`Popup.show.confirm/input/text`；`SillyTavern.libs.*`

### B. 现脚本到新模块映射

| 现脚本 | 新模块 |
|--------|--------|
| `PRICING/DEFAULT_PEAK_HOURS/MAX_HISTORY/DETAIL_KEEP` | `src/constants/pricing.ts` |
| `state` | `src/store/index.ts` + `src/store/selectors.ts` |
| `saveWithRetry/loadWithRetry/saveToLS/loadFromLS/saveData/loadData` | `src/store/persistence.ts` |
| `isWeekendDay/isPeakHour/_dsLocalDay/getModelList/mergePrices/getPricing/hasPriceForModel/fmtCost/isDeepSeekOfficialModel` | `src/services/pricing.ts` + `src/utils/date.ts` |
| `calcCost/calcSavings/recalcAllCosts` | `src/services/pricing.ts` |
| `createNewSave/getSelectedSave/getMergedStats/deleteSave/loadCurrentSave` | `src/services/saves.ts` |
| `calculateRemainingRounds` | `src/store/selectors.ts` |
| `exportHistory/normalizeImportData/importHistory/applyImportedData` | `src/services/import-export.ts` |
| `buildLocalBundle/mergeBundles/applyMerged/webdav*` | `src/services/sync.ts` |
| `patchFetch/processUsage` | `src/services/interception.ts` |
| `queryBalance/autoQueryBalance` | `src/services/balance.ts` |
| `generateDebugBatch` | `src/services/debug.ts` |
| `encryptKey/decryptKey/maskApiKey` | `src/utils/crypto.ts` |
| `PANEL_HTML/createUI/togglePanel/refreshUI/refreshSaveSelect` | `src/ui/panel.ts` |
| `createChartUI/toggleCharts/renderCharts/...` | `src/ui/charts.ts` |
| `createSettingsUI/bindSettingsControls/refreshSettingsUI/...Editor` | `src/ui/settings.ts` + `components/*` |
| `createPeakDot/updatePeakDot/getPeakDotStatus` | `src/ui/peak-dot.ts` |
| `_ds_log/_ds_toast` | `src/utils/logger.ts` |
| `checkForUpdates` | 移除（由 `manifest.auto_update` 接管，或保留为“检查扩展更新”按钮） |

### C. 术语

- **脚本**：酒馆助手（Tavern Helper）脚本，IIFE 注入式
- **扩展**：SillyTavern 原生 Extension，`manifest.json` 声明式
- **LWW**：Last-Write-Wins，最后写入获胜
- **TTFT**：Time To First Token，首字延迟

---

> 选型已全部确认（v0.2），下一步按 §10 阶段 0 初始化 `RE3.0` 空扩展（Vite + SmartTheme + ECharts + 分页 + XOR + 1.11.0，UI 对标 DeepSeek 官方）并开始阶段 1。

> 2026-08-30 阶段 0-4 已落地：空扩展→核心平移（定价/存储/拦截/余额）→导入导出/WebDAV/设置/峰值圆点→完整统计/ECharts/对比详情→抛光（显隐折叠/hooks清理/README）。当前 `RE3.0/index.js 71.8kB / style.css 2.3kB`，`typecheck` 通过。

## 变更记录

- v0.2 (2026-08-30)：确认扩展名 `API用量统计`、Vite、无框架 SmartTheme、ECharts、分页、XOR、1.11.0、峰值圆点保留、WebDAV 保留、独立仓库暂缓、脚本主线不变；新增 §7.5 DeepSeek 官方风格对标（基于推测）。
- v0.3 (2026-08-30)：基于截图精确取色：`#FFFFFF/#F6F7F8/#111827/#FF6A00/#E6F8EC`，明确 pill 按钮/浅灰卡/橙柱/薄荷标签等，更新 §7.1/7.3/7.5 为强制浅色隔离容器 + 双主题回退，信息架构 1:1 对标截图分层。
- v0.4 (2026-08-30)：明确“样式=DeepSeek，内容=脚本 1:1”：§7.2 布局与 §7.5 架构重申 23 卡/7 图/热力图/历史/对比/峰值等全部保留，不因对标而删减，仅视觉换皮。
